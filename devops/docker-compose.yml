
version: '3'
services:
  agrocadi:
    image: python:3.11
    working_dir: /app
    volumes:
      - .:/app
    command: bash -c "pip install fastapi uvicorn python-multipart && cd backend && uvicorn main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
